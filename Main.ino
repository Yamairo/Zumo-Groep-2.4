#include "Motor.h"
#include "Accelerometer.h"
/*
Motor motor;
Accelerometer accel;

void setup() {
  accel.sensorenInitialiseren();
}

void loop() {
  motor.test();
  if (accel.brugKantelingDetecteren()) {
    Serial.print("Ja");
    motor.rechteLijn();
    ledRed(1);
  }
  else {
    ledRed(0);
    motor.stop();
  }
}*/